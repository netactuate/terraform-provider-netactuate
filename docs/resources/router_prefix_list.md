---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netactuate_router_prefix_list Resource - netactuate"
subcategory: ""
description: |-
  Beta. The cloud router family is in beta: behaviour and schema may change. Manages a prefix list on a cloud router, used by BGP policy.
---

# netactuate_router_prefix_list (Resource)

Beta. The cloud router family is in beta: behaviour and schema may change. Manages a prefix list on a cloud router, used by BGP policy.

## Example Usage

```terraform
resource "netactuate_router_prefix_list" "example" {
  ip_version = 4
  name       = "example"
  router_id  = 12345
  rule {
    action = "ACCEPT"
    prefix = "192.0.2.0/24"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `ip_version` (Number) Whether the prefix list refers to IPv4 or IPv6 networks (4 or 6).
- `name` (String) Name your prefix list something you'll use to refer to it in other configurations.
- `router_id` (Number) The ID of the cloud router.
- `rule` (Block Set, Min: 1) The set of rules for the prefix list. (see [below for nested schema](#nestedblock--rule))

### Optional

- `description` (String) A description for the prefix list.

### Read-Only

- `id` (String) The ID of this resource.
- `prefix_list_id` (Number) The ID of the prefix list.

<a id="nestedblock--rule"></a>
### Nested Schema for `rule`

Required:

- `action` (String) Whether to permit or deny matching prefixes.
- `prefix` (String) The network prefix in CIDR notation (e.g. 10.0.0.0/8).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import netactuate_router_prefix_list.example 12345
```
